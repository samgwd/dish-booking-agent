## Keycloak (local) - Docker Compose environment variables
##
## Copy to `.env` in this directory (this repo ignores `.env` in git).
##
## Start:
##   docker compose --env-file keycloak.env up -d
## Logs:
##   docker compose logs -f keycloak
##   docker compose logs -f postgres

# Postgres settings for Keycloak
KEYCLOAK_POSTGRES_DB=keycloak
KEYCLOAK_POSTGRES_USER=keycloak
KEYCLOAK_POSTGRES_PASSWORD=change-me

# Initial Keycloak admin user (for the Admin Console)
KEYCLOAK_BOOTSTRAP_ADMIN_USERNAME=admin
KEYCLOAK_BOOTSTRAP_ADMIN_PASSWORD=change-me-too
