[project]
name = "dish-booking-agent"
version = "0.1.0"
description = "AI Agent for booking rooms via Dish MCP"
requires-python = ">=3.12"
dependencies = [
    "pydantic-ai>=0.0.14",
    "python-dotenv>=1.0.1",
    "httpx>=0.27.0",
    "openai>=1.55.0",
    "mcp>=1.0.0"
]

